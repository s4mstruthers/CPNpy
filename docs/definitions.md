<!-- Generated by `python -m cpnpy.mining.definitions > docs/definitions.md`.
     Edit cpnpy/mining/definitions.py, not this file. -->

# Definitions

Every property the analyses in CPNpy report, defined precisely. In the app, hover over a property in an **Analysis** tab to see its definition, or click it to keep the definition open.

Throughout, $N = (P, T, F)$ is a Petri net and, for WF-nets, $i$ and $o$ are its start and end places.

- [Notation](#notation)
- [Behavioural properties](#behavioural-properties)
- [Workflow nets and soundness](#workflow-nets-and-soundness)
- [Structural properties](#structural-properties)
- [Invariants](#invariants)
- [Footprints](#footprints)

## Notation

The basic vocabulary every other definition is written in.

### Petri net

A bipartite graph of places (circles, conditions) and transitions (boxes, actions), connected by arcs.

$$
N = (P, T, F)
$$

$$
P \cap T = \emptyset, \quad F \subseteq (P \times T) \cup (T \times P)
$$

Plain nets in the course have arc weight 1. The editor also allows a weight $W(f) \in \mathbb{N}$ on an arc $f \in F$; a transition then needs, and produces, that many tokens.

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 1.*

### Pre-set and post-set

The input nodes and the output nodes of a node.

$$
\bullet x = \lbrace y \mid (y, x) \in F \rbrace
$$

$$
x \bullet = \lbrace y \mid (x, y) \in F \rbrace
$$

For a transition $t$, $\bullet t$ are its input places and $t \bullet$ its output places.

Builds on: [Petri net](#petri-net).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Section 3.*

### Marking

The state of a net: how many tokens each place holds.

$$
M \colon P \to \mathbb{N}
$$

$$
M_1 \geq M_2 \iff \forall p \in P \colon M_1(p) \geq M_2(p)
$$

Markings are written as multisets: $[i]$ is one token in $i$, $[p_1, 2 p_2]$ is one token in $p_1$ and two in $p_2$.

Builds on: [Petri net](#petri-net).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Section 3.*

### Enabling and firing

A transition is enabled when every input place has a token. Firing it takes one token from each input place and puts one in each output place.

$$
t \text{ enabled in } M \iff \forall p \in \bullet t \colon M(p) \geq 1
$$

$$
M \xrightarrow{t} M' \iff t \text{ enabled in } M \land M' = M - \bullet t + t \bullet
$$

Builds on: [Marking](#marking), [Pre-set and post-set](#pre-set-and-post-set).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Section 3.*

### Reachable markings

The markings a net can get to by firing transitions, one after the other.

$$
M \xrightarrow{\sigma} M' \iff \sigma = \langle t_1, \ldots, t_n \rangle \land M \xrightarrow{t_1} M_1 \xrightarrow{t_2} \cdots \xrightarrow{t_n} M'
$$

$$
M \xrightarrow{*} M' \iff \exists \sigma \colon M \xrightarrow{\sigma} M'
$$

$$
R(N, M_0) = \lbrace M \mid M_0 \xrightarrow{*} M \rbrace
$$

The empty sequence is allowed, so $M \xrightarrow{*} M$ always holds. The reachability graph has the markings in $R(N, M_0)$ as nodes and an edge for every firing between them.

Builds on: [Enabling and firing](#enabling-and-firing).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Section 3.*

## Behavioural properties

Properties of a *marked* Petri net $(N, M_0)$: they depend on the initial marking and are decided on the reachability (or coverability) graph.

### Bounded

No place can ever hold more than some fixed number of tokens. Then the reachability graph is finite.

$$
(N, M_0) \text{ is } k\text{-bounded} \iff \forall M \in R(N, M_0) \; \forall p \in P \colon M(p) \leq k
$$

$$
(N, M_0) \text{ is bounded} \iff \exists k \in \mathbb{N} \colon (N, M_0) \text{ is } k\text{-bounded}
$$

If the net is unbounded, the app builds the coverability graph instead, where $\omega$ marks a place that can hold arbitrarily many tokens.

Builds on: [Reachable markings](#reachable-markings).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 3.*

### Safe

Every place holds at most one token, so a place is a condition that is either true or false.

$$
(N, M_0) \text{ is safe} \iff \forall M \in R(N, M_0) \; \forall p \in P \colon M(p) \leq 1
$$

Safe is the same as 1-bounded. A sound free-choice WF-net and a sound well-structured WF-net are always safe.

Builds on: [Bounded](#bounded).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 3, Lemmas 1 and 3.*

### Deadlock-free

No reachable marking is dead, i.e. the net can never get stuck with nothing enabled.

$$
M \text{ is dead} \iff \forall t \in T \colon t \text{ is not enabled in } M
$$

$$
(N, M_0) \text{ is deadlock-free} \iff \forall M \in R(N, M_0) \colon M \text{ is not dead}
$$

A WF-net always stops in $[o]$, which is a dead marking, so for WF-nets the question is asked about the short-circuited net $\overline{N}$, where $[o]$ enables $t^*$.

Deadlock-free is weaker than live: a net can keep firing in a loop while some transition is dead. Live implies deadlock-free (when $T \neq \emptyset$), not the other way round.

Builds on: [Reachable markings](#reachable-markings), [Short-circuited net](#short-circuited-net).

*Source: course lecture on Petri net properties.*

### Dead transition

A transition that can never fire, whatever happens first.

$$
t \text{ is dead in } (N, M_0) \iff \neg \exists M \in R(N, M_0) \colon t \text{ enabled in } M
$$

Builds on: [Reachable markings](#reachable-markings).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Section 5.*

### Live

Whatever has happened so far, every transition can still fire again later.

$$
(N, M_0) \text{ is live} \iff \forall t \in T \; \forall M \in R(N, M_0) \; \exists M' \in R(N, M) \colon t \text{ enabled in } M'
$$

On a finite reachability graph: $t$ is live iff it can fire inside every *bottom* strongly connected component (one with no edge leaving it), because every run ends up trapped in one of those.

Builds on: [Reachable markings](#reachable-markings).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 2.*

### Reversible

From every reachable marking the initial marking can be reached again.

$$
(N, M_0) \text{ is reversible} \iff \forall M \in R(N, M_0) \colon M \xrightarrow{*} M_0
$$

Builds on: [Reachable markings](#reachable-markings).

*Source: course lecture on Petri net properties.*

## Workflow nets and soundness

A WF-net models the life cycle of one case, from its start place $i$ to its end place $o$.

### WF-net

A workflow net: one start place $i$, one end place $o$, and nothing that is not on the way from $i$ to $o$.

$$
\text{(i)} \; \exists! \, i \in P \colon \bullet i = \emptyset
$$

$$
\text{(ii)} \; \exists! \, o \in P \colon o \bullet = \emptyset
$$

$$
\text{(iii)} \; \forall x \in P \cup T \colon x \text{ is on a path from } i \text{ to } o
$$

A case starts as the marking $[i]$ and should end as $[o]$.

Builds on: [Pre-set and post-set](#pre-set-and-post-set).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 11.*

### Short-circuited net

The WF-net with one extra transition $t^*$ that takes the token from $o$ back to $i$, so that finishing a case starts the next one.

$$
\overline{N} = (P, \; T \cup \lbrace t^* \rbrace, \; F \cup \lbrace (o, t^*), (t^*, i) \rbrace)
$$

$N$ is a WF-net exactly when $\overline{N}$ is strongly connected (every node can reach every other).

Builds on: [WF-net](#wf-net).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Section 5.*

### (i) Option to complete

From every marking a case can reach, it is still possible to finish.

$$
\forall M \colon [i] \xrightarrow{*} M \Rightarrow M \xrightarrow{*} [o]
$$

Violated by a deadlock (the case is stuck) or a livelock (the case can only loop forever).

Builds on: [Reachable markings](#reachable-markings), [WF-net](#wf-net).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 12 (i).*

### (ii) Proper completion

The moment a token reaches $o$, every other place is empty: nothing is left behind.

$$
\forall M \colon [i] \xrightarrow{*} M \land M \geq [o] \Rightarrow M = [o]
$$

Builds on: [Reachable markings](#reachable-markings), [WF-net](#wf-net).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 12 (ii).*

### (iii) No dead transitions

Every transition can fire in some run of the case.

$$
\forall t \in T \; \exists M, M' \colon [i] \xrightarrow{*} M \xrightarrow{t} M'
$$

Builds on: [Reachable markings](#reachable-markings), [Dead transition](#dead-transition).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 12 (iii).*

### Sound

A WF-net is sound when every case can always finish, finishes cleanly, and every transition is useful.

$$
N \text{ is sound} \iff \text{(i) option to complete} \land \text{(ii) proper completion} \land \text{(iii) no dead transitions}
$$

Checked on the reachability graph from $[i]$. A sound WF-net is always bounded, so an unbounded net is unsound straight away.

Builds on: [(i) Option to complete](#i-option-to-complete), [(ii) Proper completion](#ii-proper-completion), [(iii) No dead transitions](#iii-no-dead-transitions).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 12.*

### Soundness theorem

Soundness is the same as liveness plus boundedness of the short-circuited net.

$$
N \text{ is sound} \iff (\overline{N}, [i]) \text{ is live and bounded}
$$

Why: $t^*$ can only fire when a case has finished, so *live* includes "every case can finish" (i) and "no transition is dead" (iii). If a case could finish with tokens left behind, $t^*$ would start the next case on top of them and tokens would pile up — so *bounded* rules out improper completion (ii).

Builds on: [Sound](#sound), [Short-circuited net](#short-circuited-net), [Live](#live), [Bounded](#bounded).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Theorem 1.*

## Structural properties

Properties of the drawing alone — places, transitions and arcs, no markings. They are quick to check and point at the construct behind a problem.

### Free-choice

Transitions that share an input place have exactly the same input places, so every choice is free: it never depends on what happened in a parallel branch.

$$
\forall t_1, t_2 \in T \colon \bullet t_1 \cap \bullet t_2 \neq \emptyset \Rightarrow \bullet t_1 = \bullet t_2
$$

Soundness of a free-choice WF-net can be decided in polynomial time, and a sound free-choice WF-net is safe.

Builds on: [Pre-set and post-set](#pre-set-and-post-set).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 7, Corollary 1, Lemma 1.*

### Elementary path

A path along the arcs that visits no node twice.

$$
C = \langle n_1, \ldots, n_k \rangle \text{ with } (n_j, n_{j+1}) \in F \text{ for } 1 \leq j < k
$$

$$
C \text{ is elementary} \iff \forall j, l \colon j \neq l \Rightarrow n_j \neq n_l
$$

$$
\alpha(C) = \lbrace n_1, \ldots, n_k \rbrace
$$

Builds on: [Petri net](#petri-net).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 5.*

### Well-handled

No place and transition are joined by two separate routes. Such a pair is a *handle*: a PT-handle is a choice that is later synchronised, a TP-handle is parallel branches that are later merged as alternatives.

$$
\forall x, y \text{ (one a place, the other a transition)} \; \forall \text{ elementary paths } C_1, C_2 \text{ from } x \text{ to } y \colon
$$

$$
\alpha(C_1) \cap \alpha(C_2) = \lbrace x, y \rbrace \Rightarrow C_1 = C_2
$$

The app finds handles as two internally disjoint paths from $x$ to $y$ (Menger's theorem: a max-flow problem with capacity 1 on every node).

Builds on: [Elementary path](#elementary-path).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 13.*

### Well-structured

Every AND-split is closed by an AND-join and every OR-split by an OR-join, also around the loop through $t^*$.

$$
N \text{ is well-structured} \iff \overline{N} \text{ is well-handled}
$$

Soundness of a well-structured WF-net can be decided in polynomial time, and a sound well-structured WF-net is safe.

Builds on: [Well-handled](#well-handled), [Short-circuited net](#short-circuited-net).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 14, Corollary 2, Lemma 3.*

### State machine

Every transition has exactly one input and one output place, so a single token moves around.

$$
\forall t \in T \colon |\bullet t| = |t \bullet| = 1
$$

Builds on: [Pre-set and post-set](#pre-set-and-post-set).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 8.*

### S-component

A part of the net that behaves like one token moving around: a strongly connected state machine that keeps every arc of its places.

$$
N_s = (P_s, T_s, F_s) \text{ with } P_s \subseteq P, \; T_s \subseteq T, \; F_s \subseteq F
$$

$$
N_s \text{ is strongly connected and a state machine}
$$

$$
\forall q \in P_s \; \forall t \in T \colon ((q, t) \in F \Rightarrow (q, t) \in F_s) \land ((t, q) \in F \Rightarrow (t, q) \in F_s)
$$

Builds on: [State machine](#state-machine).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definition 9.*

### S-coverable

Every node lies in some S-component of the short-circuited net: the net is a set of threads (one "document" each) that synchronise on shared tasks.

$$
N \text{ is S-coverable} \iff \forall x \in P \cup T \cup \lbrace t^* \rbrace \; \exists \text{ S-component } N_s \text{ of } \overline{N} \colon x \in N_s
$$

Sound free-choice and sound well-structured WF-nets are S-coverable, and an S-coverable WF-net is safe. Unsound nets are often not S-coverable, so a node outside every S-component deserves a close look.

Builds on: [S-component](#s-component), [Short-circuited net](#short-circuited-net).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Definitions 10 and 16, Corollaries 3 and 4.*

### Start and end rule

A quick necessary condition for soundness: a transition that takes from $i$ takes only from $i$, and one that puts into $o$ puts only into $o$.

$$
N \text{ is sound} \Rightarrow \forall t \in T \colon (i \in \bullet t \Rightarrow \bullet t = \lbrace i \rbrace) \land (o \in t \bullet \Rightarrow t \bullet = \lbrace o \rbrace)
$$

Otherwise $t$ needs $i$ (or $o$) marked together with another place, which never happens in a sound net, so $t$ is dead.

Builds on: [Sound](#sound).

*Source: W.M.P. van der Aalst, Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques (2000), Lemma 4.*

## Invariants

Linear algebra on the drawing: what firing does to each place, written as a matrix, and the weightings and firing counts it leaves unchanged. Like the structural properties they need no state space.

### Incidence matrix

What firing each transition does to each place: the tokens it puts in minus the tokens it takes out.

$$
C(p, t) = W(t, p) - W(p, t) \quad \text{for } p \in P, \; t \in T
$$

$$
M \xrightarrow{\sigma} M' \Rightarrow M' = M + C \cdot \overline{\sigma}
$$

$W(x, y)$ is the weight of the arc from $x$ to $y$, and $0$ if there is none. $\overline{\sigma}$, the *Parikh vector* of $\sigma$, counts how often each transition occurs in $\sigma$. This *marking equation* is only a necessary condition: it ignores the order of the firings.

Builds on: [Enabling and firing](#enabling-and-firing).

*Source: T. Murata, Petri Nets: Properties, Analysis and Applications, Proceedings of the IEEE 77(4) (1989), Section VII.*

### P-invariant

A weighting of the places whose weighted token count never changes, whatever fires: a conservation law of the net.

$$
y \colon P \to \mathbb{N}, \quad y \neq 0, \quad y \cdot C = 0
$$

$$
M_0 \xrightarrow{*} M \Rightarrow y \cdot M = y \cdot M_0
$$

The app lists the *minimal* ones (no other uses a strict subset of their places); every other invariant with non-negative weights is a sum of multiples of these.

*Covered by P-invariants* (every place has a positive weight in some P-invariant) implies that the net is bounded from every initial marking. In a WF-net, $i + p + o = 1$ says: one token travels through these places.

Builds on: [Incidence matrix](#incidence-matrix), [Bounded](#bounded).

*Source: T. Murata, Petri Nets: Properties, Analysis and Applications, Proceedings of the IEEE 77(4) (1989), Section VII-A.*

### T-invariant

How often to fire each transition to end up in the marking you started from: a cycle of the behaviour.

$$
x \colon T \to \mathbb{N}, \quad x \neq 0, \quad C \cdot x = 0
$$

$$
M \xrightarrow{\sigma} M' \land \overline{\sigma} = x \Rightarrow M' = M
$$

A net that is live and bounded is *covered by T-invariants*: every transition occurs in one. By the soundness theorem, a sound WF-net's short-circuited net $\overline{N}$ is live and bounded, so a transition in no T-invariant of $\overline{N}$ proves that the WF-net is not sound. (Covered does not imply sound.)

Builds on: [Incidence matrix](#incidence-matrix), [Live](#live), [Short-circuited net](#short-circuited-net), [Soundness theorem](#soundness-theorem).

*Source: T. Murata, Petri Nets: Properties, Analysis and Applications, Proceedings of the IEEE 77(4) (1989), Section VII-A.*

## Footprints

The ordering relations of the α-algorithm, for an event log or for the behaviour of a net.

### Footprint

The ordering relations between activities: which can directly follow which.

$$
a >_L b \iff \exists \sigma = \langle t_1, \ldots, t_n \rangle \in L \; \exists j \colon t_j = a \land t_{j+1} = b
$$

$$
a \rightarrow_L b \iff a >_L b \land \neg (b >_L a)
$$

$$
a \leftarrow_L b \iff b \rightarrow_L a
$$

$$
a \parallel_L b \iff a >_L b \land b >_L a
$$

$$
a \#_L b \iff \neg (a >_L b) \land \neg (b >_L a)
$$

For a net, $L$ is the set of its complete firing sequences (visible labels only). Comparing a log's footprint with a model's is a simple conformance check.

*Source: W.M.P. van der Aalst, Process Mining: Data Science in Action (2016), Section 6.2.*

## Sources

- W.M.P. van der Aalst, *Workflow Verification: Finding Control-Flow Errors Using Petri-Net-Based Techniques* (2000).
- W.M.P. van der Aalst, *Process Mining: Data Science in Action* (2016).
